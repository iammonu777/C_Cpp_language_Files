#include <stdio.h>

void main () {
	
	// write text to file

FILE *fp;

// write mode and append mode

// Ep = fopen ("mytextfile.txt", "W");

fp = fopen ("mytextfile.txt", "a");

// fputs () ;

fputs (" world", p) ;

fclose (fp) ;
}